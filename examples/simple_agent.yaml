# Simple OpenMind Agent Configuration
# Beginner-friendly example

agent:
  name: simple_agent
  description: >
    A minimal OpenMind agent configuration
    for new developers.

model:
  provider: openmind
  name: om1-base

api:
  key: YOUR_API_KEY_HERE

settings:
  temperature: 0.7
  max_tokens: 512
